<VirtualHost *:80>
    ServerAdmin EMAIL
    
    Servername SERVERNAME

    Alias /pdf/ PATH /bases/pdf/
    Alias /img/fbpe/ PATH /img/revistas/
    Alias /img/scimago/ PATH /bases/scimago/images/
    
    DocumentRoot PATH (NOT USE LAST SLASH)
	<Directory />
		Options FollowSymLinks
		AllowOverride None
	</Directory>
    <Directory PATH (USE WITH LAST SLASH)>
        Options FollowSymLinks
        AllowOverride AuthConfig
        Order allow,deny
        Allow from all
        DirectoryIndex index.php scielo.php index.html
        Satisfy all
    </Directory>

    <Directory PATH /bases/pdf/>
        Options  FollowSymLinks MultiViews
        Order deny,allow
         AcceptPathInfo On
        Allow from all
    </Directory>

    <Directory PATH /bases/scimago/images/>
        Options  FollowSymLinks MultiViews
        Order deny,allow
         AcceptPathInfo On
        Allow from all
    </Directory>

    ScriptAlias /cgi-bin/ PATH (USE WITH LAST SLASH)
    <Directory "PATH (USE WITH LAST SLASH)">
        AllowOverride None
        Options None
        Order allow,deny
        Allow from all
    </Directory>
</VirtualHost>
